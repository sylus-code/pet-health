{% extends "base.html.twig" %}
{% block body %}
    {% include('navbar.html.twig') %}
    <div class="container">
        <div class="text-center">
            <h5 class="card-title mt-4 text-uppercase">Aktualizuj objawy</h5>
        </div>
        <div class="col-md-6 offset-md-3">
            {{ form(form) }}
            <a class="btn btn-warning mt-1" href="{{ path('symptom', {'id': animal.id }) }}" role="button">Powrót</a>
        </div>
    </div>
{% endblock %}


